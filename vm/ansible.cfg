[defaults]
# Inventory: vm/inventory/localhost.yml; play 1 builds group vm from vm_hosts_file.
# Target runs k3s; workloads are reconciled separately by Flux.
roles_path = roles
interpreter_python = auto_silent
remote_tmp = /tmp/.ansible-${USER}/tmp
